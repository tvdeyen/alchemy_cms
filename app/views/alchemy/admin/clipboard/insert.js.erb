$('#clipboard_button .icon').removeClass('fa-clipboard').addClass('fa-paste');
<% if params[:remove] -%>
  Alchemy.growl('<%= j Alchemy.t("item moved to clipboard", name: @item.name) %>');
<% else -%>
  Alchemy.growl('<%= j Alchemy.t("item copied to clipboard", name: @item.name) %>')
<% end -%>
